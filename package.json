{
  "name": "CRM_APP_KMP",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webApp",
    "sharedLogic/build/dist/js/productionLibrary",
    "sharedLogic/build/dist/js/developmentLibrary"
  ],
  "scripts": {
    "build:shared": "chmod +x ./gradlew && ./gradlew :sharedLogic:jsBrowserProductionLibraryDistribution",
    "build": "npm run build:shared && npm run build -w webApp",
    "start": "npm run build:shared && npm run start -w webApp"
  }
}