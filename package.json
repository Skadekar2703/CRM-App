{
  "name": "CRM_APP_KMP",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webApp"
  ],
  "scripts": {
    "build": "npm run build -w webApp",
    "start": "npm run start -w webApp"
  }
}