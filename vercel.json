{
  "buildCommand": "npm run build --prefix webApp",
  "outputDirectory": "webApp/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
